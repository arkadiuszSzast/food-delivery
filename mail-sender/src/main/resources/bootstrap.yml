spring:
  application:
    name: mail-sender

  cloud:
    config:
      fail-fast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20
      uri: ${CONFIG_URI:http://localhost:8888}
      username: account_actuator
      password: ${ACTUATOR_PASSWORD}

  sendgrid:
    api-key: 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

mail:
  template:
    confirmUserRegistration: d-d00f99de8c8c4fe8b697c546a0bace78
    confirmEmployeeRegistration: d-1859b817f7844f06a3cac08fe9e7fed9

sendgrid:
  api-key: ${SENDGRID_API_KEY}
  url: https://api.sendgrid.com/

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000
