spring:
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
    discovery:
      enabled: false
  kafka:
    consumer:
      bootstrap-servers: localhost:9092

sendgrid:
  api-key: ${SENDGRID_API_KEY}
  url: https://api.sendgrid.com/

actuator:
  username: admin
  password: admin
  authority: ACTUATOR
  path: /actuator/**

